    1  sudo apt update && sudo apt upgrade -y
    2  sudo swapoff -a
    3  sudo sed -i '/ swap / s/^/#/' /etc/fstab
    4  sudo modprobe overlay
    5  sudo modprobe br_netfilter
    6  cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
    7  net.bridge.bridge-nf-call-iptables = 1
    8  net.ipv4.ip_forward = 1
    9  net.bridge.bridge-nf-call-ip6tables = 1
   10  EOF
   11  sudo sysctl --system
   12  sudo apt install -y ca-certificates curl gnupg
   13  sudo install -m 0755 -d /etc/apt/keyrings
   14  curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo tee /etc/apt/keyrings/docker.gpg > /dev/null
   15  echo   "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
   16    https://download.docker.com/linux/ubuntu \
   17    $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
   18  sudo apt update
   19  sudo apt install -y docker-ce docker-ce-cli containerd.io
   20  sudo apt install docker.io -y
   21  sudo systemctl enable docker
   22  sudo systemctl start docker
   23  sudo mkdir -p /etc/apt/keyrings
   24  curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key |   sudo tee /etc/apt/keyrings/kubernetes-apt-keyring.gpg > /dev/null
   25  echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] \
   26  https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /" | sudo tee /etc/apt/sources.list.d/kubernetes.list
   27  sudo apt update
   28  sudo apt install -y kubelet kubeadm kubectl
   29  sudo rm -f /etc/apt/sources.list.d/docker.list
   30  sudo rm -f /etc/apt/sources.list.d/kubernetes.list
   31  sudo rm -rf /etc/apt/keyrings/*
   32  sudo apt update
   33  sudo apt install -y ca-certificates curl gnupg
   34  sudo mkdir -p /etc/apt/keyrings
   35  curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
   36  sudo chmod a+r /etc/apt/keyrings/docker.gpg
   37  echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
   38  https://download.docker.com/linux/ubuntu noble stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
   39  curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes.gpg
   40  sudo chmod a+r /etc/apt/keyrings/kubernetes.gpg
   41  echo "deb [signed-by=/etc/apt/keyrings/kubernetes.gpg] \
   42  https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /" | sudo tee /etc/apt/sources.list.d/kubernetes.list
   43  sudo apt update
   44  sudo apt install -y docker-ce docker-ce-cli containerd.io
   45  sudo apt install -y kubelet kubeadm kubectl
   46  sudo apt-mark hold kubelet kubeadm kubectl
   47  sudo systemctl enable docker
   48  sudo systemctl start docker
   49  sudo apt install -y docker-ce docker-ce-cli containerd.io
   50  sudo systemctl start docker
   51  sudo systemctl enable docker
   52  sudo systemctl enable kubelet
   53  kubeadm join 192.168.0.66:6443 --token g6qc89.vruwpshxbthr5hjh         --discovery-token-ca-cert-hash sha256:f3ab3a846234d18be50ee3020c22dc6e97d3cabdd7648000cbf6f69b570cd34b
   54  kubeadm join 192.168.0.66:6443 --token 3bazr5.6wsjpxxr903zvw0u --discovery-token-ca-cert-hash sha256:f3ab3a846234d18be50ee3020c22dc6e97d3cabdd7648000cbf6f69b570cd34b
   55  sudo kubeadm reset -f
   56  sudo systemctl restart kubelet
   57  sudo systemctl stop containerd
   58  sudo rm -rf /etc/containerd/config.toml
   59  sudo containerd config default | sudo tee /etc/containerd/config.toml
   60  sudo nano /etc/containerd/config.toml
   61  sudo systemctl daemon-reexec
   62  sudo systemctl restart containerd
   63  sudo systemctl enable containerd
   64  sudo systemctl restart kubelet
   65  crictl info
   66  sudo kubeadm reset -f
   67  sudo kubeadm join <MASTER_IP>:6443 --token <NEW_TOKEN> --discovery-token-ca-cert-hash sha256:<HASH>
   68  sudo journalctl -u containerd -xe
   69  kubeadm join 192.168.0.66:6443 --token 3bazr5.6wsjpxxr903zvw0u --discovery-token-ca-cert-hash sha256:f3ab3a846234d18be50ee3020c22dc6e97d3cabdd7648000cbf6f69b570cd34b
   70  sudo kubeadm join <MASTER_IP>:6443 --token <NEW_TOKEN> --discovery-token-ca-cert-hash sha256:<HASH>
   71  ifconfig
   72  sudo kubeadm reset -f
   73  sudo systemctl stop kubelet
   74  sudo systemctl stop containerd
   75  sudo apt purge -y kubeadm kubectl kubelet kubernetes-cni
   76  sudo apt autoremove -y
   77  sudo rm -rf /etc/kubernetes/
   78  sudo rm -rf /var/lib/kubelet/
   79  sudo rm -rf /var/lib/etcd/
   80  sudo rm -rf ~/.kube
   81  sudo rm -rf /etc/containerd
   82  sudo rm -rf /var/lib/containerd
   83  sudo iptables -F
   84  sudo iptables -t nat -F
   85  sudo iptables -t mangle -F
   86  sudo iptables -X
   87  sudo ip link delete cni0 || true
   88  sudo ip link delete flannel.1 || true
   89  sudo reboot
   90  sudo swapoff -a
   91  sudo sed -i '/ swap / s/^/#/' /etc/fstab
   92  sudo apt update
   93  sudo apt install -y containerd
   94  sudo mkdir -p /etc/containerd
   95  sudo containerd config default | sudo tee /etc/containerd/config.toml
   96  sudo nano /etc/containerd/config.toml
   97  sudo systemctl restart containerd
   98  sudo systemctl enable containerd
   99  sudo mkdir -p /etc/apt/keyrings
  100  curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes.gpg
  101  echo "deb [signed-by=/etc/apt/keyrings/kubernetes.gpg] \
  102  https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /" | sudo tee /etc/apt/sources.list.d/kubernetes.list
  103  sudo apt update
  104  sudo apt install -y kubelet kubeadm kubectl
  105  sudo apt-mark hold kubelet kubeadm kubectl
  106  sudo kubeadm join 192.168.0.66:6443 --token hy33fs.cuwbi7cy8xtyuwat --discovery-token-ca-cert-hash sha256:f3ab3a846234d18be50ee3020c22dc6e97d3cabdd7648000cbf6f69b570cd34b
  107  sudo modprobe br_netfilter
  108  sudo modprobe overlay
  109  cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
  110  overlay
  111  br_netfilter
  112  EOF
  113  cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
  114  net.bridge.bridge-nf-call-iptables = 1
  115  net.bridge.bridge-nf-call-ip6tables = 1
  116  net.ipv4.ip_forward = 1
  117  EOF
  118  sudo sysctl --system
  119  ls /proc/sys/net/bridge/
  120  sudo kubeadm join 192.168.0.66:6443 --token hy33fs.cuwbi7cy8xtyuwat --discovery-token-ca-cert-hash sha256:f3ab3a846234d18be50ee3020c22dc6e97d3cabdd7648000cbf6f69b570cd34b
  121  kubectl get nodes
  122  kubens
  123  kubectl get ns
  124  ping www.google.com
  125  sudo chmod 777 /etc/resolv.conf 
  126  vi /etc/resolv.conf 
  127  ping www.google.com
  128  sudo chmod 777 /etc/resolv.conf 
  129  cat /etc/resolv.conf 
  130  ping www.google.com
  131  history
  132  history > worker-node2-cmds.txt
  133  pwd
  134  ls
  135  cd ~
  136  history > worker-node2-cmds.txt
